package com.example.cm2115.model;

import java.io.IOException;

/**
 * The RewardAccount class represents an account that includes reward features.
 * It provides methods for adding rewards to the account.
 */
public class RewardAccount {

    /**
     * Adds reward points to the account.
     *
     * @param amount The amount of reward points to be added.
     * @throws IOException If an I/O error occurs during the reward points addition.
     */
    public static void addReward(int amount) throws IOException {
        // Add logic for adding reward points to the account
        // (e.g., updating a CSV file or interacting with a database).
        // Consider using the CSVHandler or other appropriate classes for data manipulation.
        // Example: Reward points could be stored in a file or database associated with the account.
    }
}
